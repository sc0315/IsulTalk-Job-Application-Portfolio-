<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="com.isul.member.MemberDAO">
  
  <select id="getMemberList" resultType="com.isul.dto.MemberDTO">
   SELECT * FROM member
  </select>		
  
  <select id="insertMember" parameterType="com.isul.dto.MemberDTO">
  	INSERT INTO member (id, password, name, phone, email, zipCode, address1, address2) 
  	VALUES (#{id}, #{password}, #{name}, #{phone}, #{email}, #{zipCode}, #{address1}, #{address2})
  </select>
  <select id="idCheck" parameterType="String" resultType="Integer">
      SELECT COUNT(*) FROM member
      WHERE id = #{id}
</select>

  
  </mapper>